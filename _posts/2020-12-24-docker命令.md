---
layout: mypost
title: docker命令
categories: [docker]

---

镜像操作

```
# 查询镜像
docker search xxx

# 拉取镜像 latest(获取最新版本)
docker pull xxx:latest

# 列出本地镜像
docker images
```

容器操作

```
# 创建并启动一个容器
# 以nginx为例 容器名称为nginx 8080:80 为为分配的端口 nginx为使用的镜像
docker run --name nginx -p 8080:80 -d nginx

# 列出正在运行的容器
docker ps

# 列出所有容器
docker ps -l

# 启动容器 
docker start [容器名称] / [容器ID]

# 停止容器
docker stop [容器名称] / [容器ID]

# 移除容器 (需先停止容器)
docker rm [容器名称] / [容器ID]

# 查看网络端口 (以上面的nginx容器为例)
docker port nginx 80

# 查看容器日志 (-f 类似 tail -f 实时输出 crtl+C 退出 )
docker logs -f [容器名称] / [容器ID]

# 查看容器进程
docker top [容器名称] / [容器ID]

# 查了容器配置和状态信息
docker inspect [容器名称] / [容器ID]


```







